<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- CONFIG_PRIMARY_KEY -->
<mapper namespace="com.hr.mapper.ConfigPrimaryKeyMapper">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="configPrimaryKeyMap" type="ConfigPrimaryKey">
        <id column="PRK_ID" property="prkId" jdbcType="VARCHAR" />
        <result column="PRIMARY_KEY_TABLE" property="primaryKeyTable" jdbcType="VARCHAR" />
        <result column="PRIMARY_KEY" property="primaryKey" jdbcType="VARCHAR" />
        <result column="KEY_NAME" property="keyName" jdbcType="VARCHAR" />
        <result column="PRIMARY_KEY_STATUS" property="primaryKeyStatus" jdbcType="VARCHAR" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        PRK_ID, PRIMARY_KEY_TABLE, PRIMARY_KEY, KEY_NAME, PRIMARY_KEY_STATUS
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findConfigPrimaryKeyAll" resultMap="configPrimaryKeyMap">
        SELECT
        <include refid="allColumns" />
        FROM CONFIG_PRIMARY_KEY
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findConfigPrimaryKeyByIds" resultMap="configPrimaryKeyMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM CONFIG_PRIMARY_KEY WHERE PRK_ID IN
        <foreach collection="list" index="index" item="prkId" open="(" separator="," close=")">
            #{prkId}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertConfigPrimaryKey" parameterType="map">
        INSERT INTO CONFIG_PRIMARY_KEY (
            PRK_ID, PRIMARY_KEY_TABLE, PRIMARY_KEY, KEY_NAME, PRIMARY_KEY_STATUS
        ) VALUES (
            #{prkId},
            #{primaryKeyTable},
            #{primaryKey},
            #{keyName},
            #{primaryKeyStatus}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateConfigPrimaryKey" parameterType="map">
        UPDATE CONFIG_PRIMARY_KEY
        <set>
            <if test="primaryKeyTable != null">
                PRIMARY_KEY_TABLE = #{primaryKeyTable},
            </if>
            <if test="primaryKey != null">
                PRIMARY_KEY = #{primaryKey},
            </if>
            <if test="keyName != null">
                KEY_NAME = #{keyName},
            </if>
            <if test="primaryKeyStatus != null">
                PRIMARY_KEY_STATUS = #{primaryKeyStatus}
            </if>
        </set>
        WHERE PRK_ID = #{prkId}
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteConfigPrimaryKeyById" parameterType="string">
        DELETE FROM CONFIG_PRIMARY_KEY WHERE PRK_ID = #{prkId}
    </delete>
    
    <!-- 根据PRIMARY_KEY_TABLE查询数据 -->
    <select id="findConfigPrimaryKeyByPrimaryKeyTable" resultMap="configPrimaryKeyMap" parameterType="String">
        SELECT
        <include refid="allColumns" />
        FROM CONFIG_PRIMARY_KEY WHERE PRIMARY_KEY_TABLE = #{tableName} and PRIMARY_KEY_STATUS = 1
    </select>
</mapper>